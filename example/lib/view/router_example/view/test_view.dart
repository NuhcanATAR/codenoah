import 'package:flutter/material.dart';

class TestRouterView extends StatelessWidget {
  const TestRouterView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Test View'),
      ),
    );
  }
}
